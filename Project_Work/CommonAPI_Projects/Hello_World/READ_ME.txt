1. package is the folder structure.

2. It does not take semicolon.

3. "provides" is the serverside entity starts with "interface name" [ packagename.interfacename ] at the begining along with "as"  instance name of the server.

4. "consumes" is the clientside entity starts with "interface name" at the begining alongwith server component name :: instance name and "as" clientInstance name.
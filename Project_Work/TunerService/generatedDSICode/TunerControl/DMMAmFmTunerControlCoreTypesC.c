/**********************************************************************
 *  Project       Harman Car Multimedia System
 *  (c) copyright 2014
 *  Company       Harman/Becker Automotive Systems GmbH
 *                All rights reserved
 **********************************************************************/

/*
 * Generated by DSI Generator version 2.0
 * Date: 26.11.2014
 */

#include "DMMAmFmTunerControlCoreTypes.h"
#include <string.h>
#include <malloc.h>

void DMMAmFmTunerControlCoreTypes_engHwBasicData_Copy( DMMAmFmTunerControlCoreTypes_engHwBasicData* lhs, const DMMAmFmTunerControlCoreTypes_engHwBasicData* rhs )
{
   if ((lhs == 0) || (rhs == 0))
      return;
   lhs->frequency = rhs->frequency ;
   lhs->pi = rhs->pi ;
   if (rhs->ps != 0)
   {
      lhs->ps = strdup(rhs->ps);
   }
   lhs->qual = rhs->qual ;
   lhs->fieldstrength = rhs->fieldstrength ;
   lhs->bitErrorRate = rhs->bitErrorRate ;
   lhs->multipath = rhs->multipath ;
   lhs->wave = rhs->wave ;
}

void DMMAmFmTunerControlCoreTypes_engAfList_Copy( DMMAmFmTunerControlCoreTypes_engAfList* lhs, const DMMAmFmTunerControlCoreTypes_engAfList* rhs )
{
   int vecsize = 0;
   if ((lhs == 0) || (rhs == 0))
      return;
   vecsize = DSIVectorSize( *rhs ) ;
   *lhs = 0 ;
   if( vecsize )
   {
      int idx ;
      DSIVectorAlloc( *lhs, DSIVectorSize( *rhs ) );

      for( idx=0; idx<vecsize; idx++ )
      {
         (*lhs)->data[idx] = (*rhs)->data[idx] ;
      }
   }
}

void DMMAmFmTunerControlCoreTypes_engTmcStation_Copy( DMMAmFmTunerControlCoreTypes_engTmcStation* lhs, const DMMAmFmTunerControlCoreTypes_engTmcStation* rhs )
{
   if ((lhs == 0) || (rhs == 0))
      return;
   lhs->frequency = rhs->frequency ;
   lhs->pi = rhs->pi ;
   lhs->sid = rhs->sid ;
   lhs->ltn = rhs->ltn ;
   lhs->qual = rhs->qual ;
   if (rhs->spn != 0)
   {
      lhs->spn = strdup(rhs->spn);
   }
}

void DMMAmFmTunerControlCoreTypes_engTmcList_Copy( DMMAmFmTunerControlCoreTypes_engTmcList* lhs, const DMMAmFmTunerControlCoreTypes_engTmcList* rhs )
{
   int vecsize = 0;
   if ((lhs == 0) || (rhs == 0))
      return;
   vecsize = DSIVectorSize( *rhs ) ;
   *lhs = 0 ;
   if( vecsize )
   {
      int idx ;
      DSIVectorAlloc( *lhs, DSIVectorSize( *rhs ) );

      for( idx=0; idx<vecsize; idx++ )
      {
         DMMAmFmTunerControlCoreTypes_engTmcStation_Copy( &(*lhs)->data[idx], &(*rhs)->data[idx] );
      }
   }
}

void DMMAmFmTunerControlCoreTypes_engAssignedTmcStation_Copy( DMMAmFmTunerControlCoreTypes_engAssignedTmcStation* lhs, const DMMAmFmTunerControlCoreTypes_engAssignedTmcStation* rhs )
{
   if ((lhs == 0) || (rhs == 0))
      return;
   lhs->tuner = rhs->tuner ;
   lhs->filterMembership = rhs->filterMembership ;
   lhs->frequency = rhs->frequency ;
   lhs->pi = rhs->pi ;
   lhs->qual = rhs->qual ;
   lhs->fs = rhs->fs ;
   lhs->ber = rhs->ber ;
   lhs->cc = rhs->cc ;
   lhs->sid = rhs->sid ;
   lhs->ltn = rhs->ltn ;
   if (rhs->ps != 0)
   {
      lhs->ps = strdup(rhs->ps);
   }
   if (rhs->spn != 0)
   {
      lhs->spn = strdup(rhs->spn);
   }
}

void DMMAmFmTunerControlCoreTypes_engTmcAssignments_Copy( DMMAmFmTunerControlCoreTypes_engTmcAssignments* lhs, const DMMAmFmTunerControlCoreTypes_engTmcAssignments* rhs )
{
   int vecsize = 0;
   if ((lhs == 0) || (rhs == 0))
      return;
   vecsize = DSIVectorSize( *rhs ) ;
   *lhs = 0 ;
   if( vecsize )
   {
      int idx ;
      DSIVectorAlloc( *lhs, DSIVectorSize( *rhs ) );

      for( idx=0; idx<vecsize; idx++ )
      {
         DMMAmFmTunerControlCoreTypes_engAssignedTmcStation_Copy( &(*lhs)->data[idx], &(*rhs)->data[idx] );
      }
   }
}

void DMMAmFmTunerControlCoreTypes_engTmcFilterList_Copy( DMMAmFmTunerControlCoreTypes_engTmcFilterList* lhs, const DMMAmFmTunerControlCoreTypes_engTmcFilterList* rhs )
{
   int vecsize = 0;
   if ((lhs == 0) || (rhs == 0))
      return;
   vecsize = DSIVectorSize( *rhs ) ;
   *lhs = 0 ;
   if( vecsize )
   {
      int idx ;
      DSIVectorAlloc( *lhs, DSIVectorSize( *rhs ) );

      for( idx=0; idx<vecsize; idx++ )
      {
         (*lhs)->data[idx] = (*rhs)->data[idx] ;
      }
   }
}

void DMMAmFmTunerControlCoreTypes_engStation_Copy( DMMAmFmTunerControlCoreTypes_engStation* lhs, const DMMAmFmTunerControlCoreTypes_engStation* rhs )
{
   if ((lhs == 0) || (rhs == 0))
      return;
   lhs->frequency = rhs->frequency ;
   lhs->pi = rhs->pi ;
   lhs->qual = rhs->qual ;
   lhs->fs = rhs->fs ;
   if (rhs->ps != 0)
   {
      lhs->ps = strdup(rhs->ps);
   }
}

void DMMAmFmTunerControlCoreTypes_engAmFmStationList_Copy( DMMAmFmTunerControlCoreTypes_engAmFmStationList* lhs, const DMMAmFmTunerControlCoreTypes_engAmFmStationList* rhs )
{
   int vecsize = 0;
   if ((lhs == 0) || (rhs == 0))
      return;
   vecsize = DSIVectorSize( *rhs ) ;
   *lhs = 0 ;
   if( vecsize )
   {
      int idx ;
      DSIVectorAlloc( *lhs, DSIVectorSize( *rhs ) );

      for( idx=0; idx<vecsize; idx++ )
      {
         DMMAmFmTunerControlCoreTypes_engStation_Copy( &(*lhs)->data[idx], &(*rhs)->data[idx] );
      }
   }
}

void DMMAmFmTunerControlCoreTypes_engHdBers_Copy( DMMAmFmTunerControlCoreTypes_engHdBers* lhs, const DMMAmFmTunerControlCoreTypes_engHdBers* rhs )
{
   int vecsize = 0;
   if ((lhs == 0) || (rhs == 0))
      return;
   vecsize = DSIVectorSize( *rhs ) ;
   *lhs = 0 ;
   if( vecsize )
   {
      int idx ;
      DSIVectorAlloc( *lhs, DSIVectorSize( *rhs ) );

      for( idx=0; idx<vecsize; idx++ )
      {
         (*lhs)->data[idx] = (*rhs)->data[idx] ;
      }
   }
}

void DMMAmFmTunerControlCoreTypes_engHdHwData_Copy( DMMAmFmTunerControlCoreTypes_engHdHwData* lhs, const DMMAmFmTunerControlCoreTypes_engHdHwData* rhs )
{
   if ((lhs == 0) || (rhs == 0))
      return;
   lhs->frequency = rhs->frequency ;
   lhs->pi = rhs->pi ;
   lhs->stationID = rhs->stationID ;
   lhs->qual = rhs->qual ;
   lhs->digitalQual = rhs->digitalQual ;
   lhs->aerial = rhs->aerial ;
   lhs->fieldstrength = rhs->fieldstrength ;
   if (rhs->shortStationName != 0)
   {
      lhs->shortStationName = strdup(rhs->shortStationName);
   }
   lhs->sps = rhs->sps ;
   lhs->acquisitionStatus = rhs->acquisitionStatus ;
}

void DMMAmFmTunerControlCoreTypes_engHdPsdData_Copy( DMMAmFmTunerControlCoreTypes_engHdPsdData* lhs, const DMMAmFmTunerControlCoreTypes_engHdPsdData* rhs )
{
   if ((lhs == 0) || (rhs == 0))
      return;
   if (rhs->Title != 0)
   {
      lhs->Title = strdup(rhs->Title);
   }
   if (rhs->Artist != 0)
   {
      lhs->Artist = strdup(rhs->Artist);
   }
   if (rhs->Album != 0)
   {
      lhs->Album = strdup(rhs->Album);
   }
   if (rhs->Genre != 0)
   {
      lhs->Genre = strdup(rhs->Genre);
   }
   if (rhs->Comment_Language != 0)
   {
      lhs->Comment_Language = strdup(rhs->Comment_Language);
   }
   if (rhs->Comment_ShortDescription != 0)
   {
      lhs->Comment_ShortDescription = strdup(rhs->Comment_ShortDescription);
   }
   if (rhs->Comment_ContentField != 0)
   {
      lhs->Comment_ContentField = strdup(rhs->Comment_ContentField);
   }
   if (rhs->Commercial_Price != 0)
   {
      lhs->Commercial_Price = strdup(rhs->Commercial_Price);
   }
   if (rhs->Commercial_ValidUntil != 0)
   {
      lhs->Commercial_ValidUntil = strdup(rhs->Commercial_ValidUntil);
   }
   if (rhs->Commercial_ContactUrl != 0)
   {
      lhs->Commercial_ContactUrl = strdup(rhs->Commercial_ContactUrl);
   }
   if (rhs->Commercial_NameOfSeller != 0)
   {
      lhs->Commercial_NameOfSeller = strdup(rhs->Commercial_NameOfSeller);
   }
   if (rhs->Commercial_Description != 0)
   {
      lhs->Commercial_Description = strdup(rhs->Commercial_Description);
   }
}



void DSIReadDMMAmFmTunerControlCoreTypes_programId( DSIStream *stream, DMMAmFmTunerControlCoreTypes_programId *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead16( stream, &(p)->code ) ;
   DSIRead32( stream, &(p)->state ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_programId( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_programId *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite16( stream, &(p)->code ) ;
   DSIWrite32( stream, &(p)->state ) ;
}

void DMMAmFmTunerControlCoreTypes_engHwBasicData_Free( DMMAmFmTunerControlCoreTypes_engHwBasicData *p )
{
   if (p == 0)
      return;
   DSIStringFree( p->ps );
   memset( p, 0, sizeof(*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engHwBasicData( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engHwBasicData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead32( stream, &(p)->frequency ) ;
   DSIReadDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIReadString( stream, &(p)->ps ) ;
   DSIRead8( stream, &(p)->qual ) ;
   DSIRead16( stream, &(p)->fieldstrength ) ;
   DSIRead8( stream, &(p)->bitErrorRate ) ;
   DSIRead8( stream, &(p)->multipath ) ;
   DSIRead32( stream, &(p)->wave ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engHwBasicData( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engHwBasicData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite32( stream, &(p)->frequency ) ;
   DSIWriteDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIWriteString( stream, &(p)->ps ) ;
   DSIWrite8( stream, &(p)->qual ) ;
   DSIWrite16( stream, &(p)->fieldstrength ) ;
   DSIWrite8( stream, &(p)->bitErrorRate ) ;
   DSIWrite8( stream, &(p)->multipath ) ;
   DSIWrite32( stream, &(p)->wave ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_engHwEnhancedData( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engHwEnhancedData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead16( stream, &(p)->adjacentChannel ) ;
   DSIRead16( stream, &(p)->fmDeviation ) ;
   DSIRead32( stream, &(p)->fmOffset ) ;
   DSIRead32( stream, &(p)->zfBandwidth ) ;
   DSIRead8( stream, &(p)->autoGainControl ) ;
   DSIRead8( stream, &(p)->oscInjection ) ;
   DSIRead32( stream, &(p)->highCut ) ;
   DSIRead8( stream, &(p)->snr ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engHwEnhancedData( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engHwEnhancedData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite16( stream, &(p)->adjacentChannel ) ;
   DSIWrite16( stream, &(p)->fmDeviation ) ;
   DSIWrite32( stream, &(p)->fmOffset ) ;
   DSIWrite32( stream, &(p)->zfBandwidth ) ;
   DSIWrite8( stream, &(p)->autoGainControl ) ;
   DSIWrite8( stream, &(p)->oscInjection ) ;
   DSIWrite32( stream, &(p)->highCut ) ;
   DSIWrite8( stream, &(p)->snr ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_engHwTrafficInfo( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engHwTrafficInfo *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead8( stream, &(p)->tp ) ;
   DSIRead8( stream, &(p)->ta ) ;
   DSIRead8( stream, &(p)->tmc ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engHwTrafficInfo( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engHwTrafficInfo *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite8( stream, &(p)->tp ) ;
   DSIWrite8( stream, &(p)->ta ) ;
   DSIWrite8( stream, &(p)->tmc ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_engAf( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engAf *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead32( stream, &(p)->frequency ) ;
   DSIReadDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIRead8( stream, &(p)->qual ) ;
   DSIRead16( stream, &(p)->fs ) ;
   DSIRead8( stream, &(p)->ber ) ;
   DSIRead8( stream, &(p)->rdsAf ) ;
   DSIRead8( stream, &(p)->inhibit ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engAf( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engAf *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite32( stream, &(p)->frequency ) ;
   DSIWriteDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIWrite8( stream, &(p)->qual ) ;
   DSIWrite16( stream, &(p)->fs ) ;
   DSIWrite8( stream, &(p)->ber ) ;
   DSIWrite8( stream, &(p)->rdsAf ) ;
   DSIWrite8( stream, &(p)->inhibit ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_engAfList( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engAfList *p )
{
   if( p && stream)
   {
      int size = 0 ;
      DMMAmFmTunerControlCoreTypes_engAfList_Free( p );

      DSIRead32( stream, &size );
      if( 0 != size )
      {
         int idx = 0 ;
         DMMAmFmTunerControlCoreTypes_engAfList vec = 0 ; 
         DSIVectorAlloc( vec, size );//lint !e449
         if (vec)
         { 
            memset( vec->data, 0, sizeof(vec->data[0]) * size );
            while( idx < vec->size )
            {
               DSIReadDMMAmFmTunerControlCoreTypes_engAf( stream, &vec->data[idx] ) ;
               idx++;
            }
         }
         *p = vec ;
      }
   }
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engAfList( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engAfList *p )
{
   if (!stream)
      return;
   if( !p || !(*p) || !(*p)->size )
   {
      static const int size0 = 0 ;
      DSIWrite32( stream, &size0 );
   }
   else
   {
      int idx = 0 ;
      const DMMAmFmTunerControlCoreTypes_engAfList vec = *p ;
      DSIWrite32( stream, &vec->size );
      while( idx < vec->size )
      {
         DSIWriteDMMAmFmTunerControlCoreTypes_engAf( stream, &vec->data[idx] ) ;
         idx++;
      }
   }
}


void DMMAmFmTunerControlCoreTypes_engTmcStation_Free( DMMAmFmTunerControlCoreTypes_engTmcStation *p )
{
   if (p == 0)
      return;
   DSIStringFree( p->spn );
   memset( p, 0, sizeof(*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engTmcStation( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engTmcStation *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead32( stream, &(p)->frequency ) ;
   DSIReadDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIRead8( stream, &(p)->sid ) ;
   DSIRead8( stream, &(p)->ltn ) ;
   DSIRead8( stream, &(p)->qual ) ;
   DSIReadString( stream, &(p)->spn ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engTmcStation( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engTmcStation *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite32( stream, &(p)->frequency ) ;
   DSIWriteDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIWrite8( stream, &(p)->sid ) ;
   DSIWrite8( stream, &(p)->ltn ) ;
   DSIWrite8( stream, &(p)->qual ) ;
   DSIWriteString( stream, &(p)->spn ) ;
}

void DMMAmFmTunerControlCoreTypes_engTmcList_Free( DMMAmFmTunerControlCoreTypes_engTmcList *p )
{
if (p == 0)
   return;
   {
      int idx = DSIVectorSize( (*p) ) ;
      while( --idx >= 0 )
      {
         DMMAmFmTunerControlCoreTypes_engTmcStation_Free( &(*p)->data[idx] );
      }
   }
   DSIVectorFree( (*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engTmcList( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engTmcList *p )
{
   if( p && stream)
   {
      int size = 0 ;
      DMMAmFmTunerControlCoreTypes_engTmcList_Free( p );

      DSIRead32( stream, &size );
      if( 0 != size )
      {
         int idx = 0 ;
         DMMAmFmTunerControlCoreTypes_engTmcList vec = 0 ; 
         DSIVectorAlloc( vec, size );//lint !e449
         if (vec)
         { 
            memset( vec->data, 0, sizeof(vec->data[0]) * size );
            while( idx < vec->size )
            {
               DSIReadDMMAmFmTunerControlCoreTypes_engTmcStation( stream, &vec->data[idx] ) ;
               idx++;
            }
         }
         *p = vec ;
      }
   }
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engTmcList( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engTmcList *p )
{
   if (!stream)
      return;
   if( !p || !(*p) || !(*p)->size )
   {
      static const int size0 = 0 ;
      DSIWrite32( stream, &size0 );
   }
   else
   {
      int idx = 0 ;
      const DMMAmFmTunerControlCoreTypes_engTmcList vec = *p ;
      DSIWrite32( stream, &vec->size );
      while( idx < vec->size )
      {
         DSIWriteDMMAmFmTunerControlCoreTypes_engTmcStation( stream, &vec->data[idx] ) ;
         idx++;
      }
   }
}


void DMMAmFmTunerControlCoreTypes_engAssignedTmcStation_Free( DMMAmFmTunerControlCoreTypes_engAssignedTmcStation *p )
{
   if (p == 0)
      return;
   DSIStringFree( p->ps );
   DSIStringFree( p->spn );
   memset( p, 0, sizeof(*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engAssignedTmcStation( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engAssignedTmcStation *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead8( stream, &(p)->tuner ) ;
   DSIRead8( stream, &(p)->filterMembership ) ;
   DSIRead32( stream, &(p)->frequency ) ;
   DSIReadDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIRead8( stream, &(p)->qual ) ;
   DSIRead16( stream, &(p)->fs ) ;
   DSIRead8( stream, &(p)->ber ) ;
   DSIRead8( stream, &(p)->cc ) ;
   DSIRead8( stream, &(p)->sid ) ;
   DSIRead8( stream, &(p)->ltn ) ;
   DSIReadString( stream, &(p)->ps ) ;
   DSIReadString( stream, &(p)->spn ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engAssignedTmcStation( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engAssignedTmcStation *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite8( stream, &(p)->tuner ) ;
   DSIWrite8( stream, &(p)->filterMembership ) ;
   DSIWrite32( stream, &(p)->frequency ) ;
   DSIWriteDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIWrite8( stream, &(p)->qual ) ;
   DSIWrite16( stream, &(p)->fs ) ;
   DSIWrite8( stream, &(p)->ber ) ;
   DSIWrite8( stream, &(p)->cc ) ;
   DSIWrite8( stream, &(p)->sid ) ;
   DSIWrite8( stream, &(p)->ltn ) ;
   DSIWriteString( stream, &(p)->ps ) ;
   DSIWriteString( stream, &(p)->spn ) ;
}

void DMMAmFmTunerControlCoreTypes_engTmcAssignments_Free( DMMAmFmTunerControlCoreTypes_engTmcAssignments *p )
{
if (p == 0)
   return;
   {
      int idx = DSIVectorSize( (*p) ) ;
      while( --idx >= 0 )
      {
         DMMAmFmTunerControlCoreTypes_engAssignedTmcStation_Free( &(*p)->data[idx] );
      }
   }
   DSIVectorFree( (*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engTmcAssignments( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engTmcAssignments *p )
{
   if( p && stream)
   {
      int size = 0 ;
      DMMAmFmTunerControlCoreTypes_engTmcAssignments_Free( p );

      DSIRead32( stream, &size );
      if( 0 != size )
      {
         int idx = 0 ;
         DMMAmFmTunerControlCoreTypes_engTmcAssignments vec = 0 ; 
         DSIVectorAlloc( vec, size );//lint !e449
         if (vec)
         { 
            memset( vec->data, 0, sizeof(vec->data[0]) * size );
            while( idx < vec->size )
            {
               DSIReadDMMAmFmTunerControlCoreTypes_engAssignedTmcStation( stream, &vec->data[idx] ) ;
               idx++;
            }
         }
         *p = vec ;
      }
   }
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engTmcAssignments( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engTmcAssignments *p )
{
   if (!stream)
      return;
   if( !p || !(*p) || !(*p)->size )
   {
      static const int size0 = 0 ;
      DSIWrite32( stream, &size0 );
   }
   else
   {
      int idx = 0 ;
      const DMMAmFmTunerControlCoreTypes_engTmcAssignments vec = *p ;
      DSIWrite32( stream, &vec->size );
      while( idx < vec->size )
      {
         DSIWriteDMMAmFmTunerControlCoreTypes_engAssignedTmcStation( stream, &vec->data[idx] ) ;
         idx++;
      }
   }
}



void DSIReadDMMAmFmTunerControlCoreTypes_engTmcFilterData( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engTmcFilterData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead8( stream, &(p)->number ) ;
   DSIRead32( stream, &(p)->mask ) ;
   DSIRead32( stream, &(p)->programId ) ;
   DSIRead16( stream, &(p)->serviceId ) ;
   DSIRead8( stream, &(p)->messageGeoScope ) ;
   DSIRead16( stream, &(p)->countryCode ) ;
   DSIRead8( stream, &(p)->locationTable ) ;
   DSIRead16( stream, &(p)->broadcastServiceArea ) ;
   DSIRead8( stream, &(p)->serviceType ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engTmcFilterData( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engTmcFilterData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite8( stream, &(p)->number ) ;
   DSIWrite32( stream, &(p)->mask ) ;
   DSIWrite32( stream, &(p)->programId ) ;
   DSIWrite16( stream, &(p)->serviceId ) ;
   DSIWrite8( stream, &(p)->messageGeoScope ) ;
   DSIWrite16( stream, &(p)->countryCode ) ;
   DSIWrite8( stream, &(p)->locationTable ) ;
   DSIWrite16( stream, &(p)->broadcastServiceArea ) ;
   DSIWrite8( stream, &(p)->serviceType ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_engTmcFilterList( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engTmcFilterList *p )
{
   if( p && stream)
   {
      int size = 0 ;
      DMMAmFmTunerControlCoreTypes_engTmcFilterList_Free( p );

      DSIRead32( stream, &size );
      if( 0 != size )
      {
         int idx = 0 ;
         DMMAmFmTunerControlCoreTypes_engTmcFilterList vec = 0 ; 
         DSIVectorAlloc( vec, size );//lint !e449
         if (vec)
         { 
            memset( vec->data, 0, sizeof(vec->data[0]) * size );
            while( idx < vec->size )
            {
               DSIReadDMMAmFmTunerControlCoreTypes_engTmcFilterData( stream, &vec->data[idx] ) ;
               idx++;
            }
         }
         *p = vec ;
      }
   }
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engTmcFilterList( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engTmcFilterList *p )
{
   if (!stream)
      return;
   if( !p || !(*p) || !(*p)->size )
   {
      static const int size0 = 0 ;
      DSIWrite32( stream, &size0 );
   }
   else
   {
      int idx = 0 ;
      const DMMAmFmTunerControlCoreTypes_engTmcFilterList vec = *p ;
      DSIWrite32( stream, &vec->size );
      while( idx < vec->size )
      {
         DSIWriteDMMAmFmTunerControlCoreTypes_engTmcFilterData( stream, &vec->data[idx] ) ;
         idx++;
      }
   }
}


void DMMAmFmTunerControlCoreTypes_engStation_Free( DMMAmFmTunerControlCoreTypes_engStation *p )
{
   if (p == 0)
      return;
   DSIStringFree( p->ps );
   memset( p, 0, sizeof(*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engStation( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engStation *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead32( stream, &(p)->frequency ) ;
   DSIReadDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIRead8( stream, &(p)->qual ) ;
   DSIRead16( stream, &(p)->fs ) ;
   DSIReadString( stream, &(p)->ps ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engStation( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engStation *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite32( stream, &(p)->frequency ) ;
   DSIWriteDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIWrite8( stream, &(p)->qual ) ;
   DSIWrite16( stream, &(p)->fs ) ;
   DSIWriteString( stream, &(p)->ps ) ;
}

void DMMAmFmTunerControlCoreTypes_engAmFmStationList_Free( DMMAmFmTunerControlCoreTypes_engAmFmStationList *p )
{
if (p == 0)
   return;
   {
      int idx = DSIVectorSize( (*p) ) ;
      while( --idx >= 0 )
      {
         DMMAmFmTunerControlCoreTypes_engStation_Free( &(*p)->data[idx] );
      }
   }
   DSIVectorFree( (*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engAmFmStationList( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engAmFmStationList *p )
{
   if( p && stream)
   {
      int size = 0 ;
      DMMAmFmTunerControlCoreTypes_engAmFmStationList_Free( p );

      DSIRead32( stream, &size );
      if( 0 != size )
      {
         int idx = 0 ;
         DMMAmFmTunerControlCoreTypes_engAmFmStationList vec = 0 ; 
         DSIVectorAlloc( vec, size );//lint !e449
         if (vec)
         { 
            memset( vec->data, 0, sizeof(vec->data[0]) * size );
            while( idx < vec->size )
            {
               DSIReadDMMAmFmTunerControlCoreTypes_engStation( stream, &vec->data[idx] ) ;
               idx++;
            }
         }
         *p = vec ;
      }
   }
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engAmFmStationList( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engAmFmStationList *p )
{
   if (!stream)
      return;
   if( !p || !(*p) || !(*p)->size )
   {
      static const int size0 = 0 ;
      DSIWrite32( stream, &size0 );
   }
   else
   {
      int idx = 0 ;
      const DMMAmFmTunerControlCoreTypes_engAmFmStationList vec = *p ;
      DSIWrite32( stream, &vec->size );
      while( idx < vec->size )
      {
         DSIWriteDMMAmFmTunerControlCoreTypes_engStation( stream, &vec->data[idx] ) ;
         idx++;
      }
   }
}



void DSIReadDMMAmFmTunerControlCoreTypes_hdBerInfo( DSIStream *stream, DMMAmFmTunerControlCoreTypes_hdBerInfo *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead8( stream, &(p)->mantisse ) ;
   DSIRead8( stream, &(p)->exponent ) ;
   DSIRead32( stream, &(p)->number ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_hdBerInfo( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_hdBerInfo *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite8( stream, &(p)->mantisse ) ;
   DSIWrite8( stream, &(p)->exponent ) ;
   DSIWrite32( stream, &(p)->number ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_engHdBers( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engHdBers *p )
{
   if( p && stream)
   {
      int size = 0 ;
      DMMAmFmTunerControlCoreTypes_engHdBers_Free( p );

      DSIRead32( stream, &size );
      if( 0 != size )
      {
         int idx = 0 ;
         DMMAmFmTunerControlCoreTypes_engHdBers vec = 0 ; 
         DSIVectorAlloc( vec, size );//lint !e449
         if (vec)
         { 
            memset( vec->data, 0, sizeof(vec->data[0]) * size );
            while( idx < vec->size )
            {
               DSIReadDMMAmFmTunerControlCoreTypes_hdBerInfo( stream, &vec->data[idx] ) ;
               idx++;
            }
         }
         *p = vec ;
      }
   }
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engHdBers( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engHdBers *p )
{
   if (!stream)
      return;
   if( !p || !(*p) || !(*p)->size )
   {
      static const int size0 = 0 ;
      DSIWrite32( stream, &size0 );
   }
   else
   {
      int idx = 0 ;
      const DMMAmFmTunerControlCoreTypes_engHdBers vec = *p ;
      DSIWrite32( stream, &vec->size );
      while( idx < vec->size )
      {
         DSIWriteDMMAmFmTunerControlCoreTypes_hdBerInfo( stream, &vec->data[idx] ) ;
         idx++;
      }
   }
}



void DSIReadDMMAmFmTunerControlCoreTypes_hdBitRates( DSIStream *stream, DMMAmFmTunerControlCoreTypes_hdBitRates *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead32( stream, &(p)->total ) ;
   DSIRead32( stream, &(p)->audio ) ;
   DSIRead32( stream, &(p)->data ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_hdBitRates( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_hdBitRates *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite32( stream, &(p)->total ) ;
   DSIWrite32( stream, &(p)->audio ) ;
   DSIWrite32( stream, &(p)->data ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_engHdPerformanceData( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engHdPerformanceData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead16( stream, &(p)->sisFrameAcquisitionTime ) ;
   DSIRead16( stream, &(p)->audioAcquisitionTime ) ;
   DSIRead8( stream, &(p)->signalToNoiseRatio ) ;
   DSIRead16( stream, &(p)->analogTime ) ;
   DSIReadDMMAmFmTunerControlCoreTypes_hdBitRates( stream, &(p)->bitRates ) ;
   DSIRead16( stream, &(p)->blendCount ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engHdPerformanceData( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engHdPerformanceData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite16( stream, &(p)->sisFrameAcquisitionTime ) ;
   DSIWrite16( stream, &(p)->audioAcquisitionTime ) ;
   DSIWrite8( stream, &(p)->signalToNoiseRatio ) ;
   DSIWrite16( stream, &(p)->analogTime ) ;
   DSIWriteDMMAmFmTunerControlCoreTypes_hdBitRates( stream, &(p)->bitRates ) ;
   DSIWrite16( stream, &(p)->blendCount ) ;
}

void DMMAmFmTunerControlCoreTypes_engHdHwData_Free( DMMAmFmTunerControlCoreTypes_engHdHwData *p )
{
   if (p == 0)
      return;
   DSIStringFree( p->shortStationName );
   memset( p, 0, sizeof(*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engHdHwData( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engHdHwData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead32( stream, &(p)->frequency ) ;
   DSIReadDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIRead32( stream, &(p)->stationID ) ;
   DSIRead8( stream, &(p)->qual ) ;
   DSIRead16( stream, &(p)->digitalQual ) ;
   DSIRead8( stream, &(p)->aerial ) ;
   DSIRead16( stream, &(p)->fieldstrength ) ;
   DSIReadString( stream, &(p)->shortStationName ) ;
   DSIRead8( stream, &(p)->sps ) ;
   DSIRead8( stream, &(p)->acquisitionStatus ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engHdHwData( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engHdHwData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite32( stream, &(p)->frequency ) ;
   DSIWriteDMMAmFmTunerControlCoreTypes_programId( stream, &(p)->pi ) ;
   DSIWrite32( stream, &(p)->stationID ) ;
   DSIWrite8( stream, &(p)->qual ) ;
   DSIWrite16( stream, &(p)->digitalQual ) ;
   DSIWrite8( stream, &(p)->aerial ) ;
   DSIWrite16( stream, &(p)->fieldstrength ) ;
   DSIWriteString( stream, &(p)->shortStationName ) ;
   DSIWrite8( stream, &(p)->sps ) ;
   DSIWrite8( stream, &(p)->acquisitionStatus ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_engHdStatusData( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engHdStatusData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead8( stream, &(p)->primaryServiceMode ) ;
   DSIRead8( stream, &(p)->acquisitionStatus ) ;
   DSIRead8( stream, &(p)->sisCrcStatus ) ;
   DSIRead8( stream, &(p)->digitalAudioAcquired ) ;
   DSIRead8( stream, &(p)->txBlendEnabled ) ;
   DSIRead8( stream, &(p)->codecMode ) ;
   DSIRead8( stream, &(p)->txDigitalAudioGain ) ;
   DSIRead8( stream, &(p)->txBlendCtrlStatus ) ;
   DSIRead16( stream, &(p)->qualityIndicator ) ;
   DSIRead8( stream, &(p)->serviceNumber ) ;
   DSIRead8( stream, &(p)->ballGameMode ) ;
   DSIRead8( stream, &(p)->taggingReadiness ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engHdStatusData( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engHdStatusData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite8( stream, &(p)->primaryServiceMode ) ;
   DSIWrite8( stream, &(p)->acquisitionStatus ) ;
   DSIWrite8( stream, &(p)->sisCrcStatus ) ;
   DSIWrite8( stream, &(p)->digitalAudioAcquired ) ;
   DSIWrite8( stream, &(p)->txBlendEnabled ) ;
   DSIWrite8( stream, &(p)->codecMode ) ;
   DSIWrite8( stream, &(p)->txDigitalAudioGain ) ;
   DSIWrite8( stream, &(p)->txBlendCtrlStatus ) ;
   DSIWrite16( stream, &(p)->qualityIndicator ) ;
   DSIWrite8( stream, &(p)->serviceNumber ) ;
   DSIWrite8( stream, &(p)->ballGameMode ) ;
   DSIWrite8( stream, &(p)->taggingReadiness ) ;
}

void DMMAmFmTunerControlCoreTypes_engHdPsdData_Free( DMMAmFmTunerControlCoreTypes_engHdPsdData *p )
{
   if (p == 0)
      return;
   DSIStringFree( p->Title );
   DSIStringFree( p->Artist );
   DSIStringFree( p->Album );
   DSIStringFree( p->Genre );
   DSIStringFree( p->Comment_Language );
   DSIStringFree( p->Comment_ShortDescription );
   DSIStringFree( p->Comment_ContentField );
   DSIStringFree( p->Commercial_Price );
   DSIStringFree( p->Commercial_ValidUntil );
   DSIStringFree( p->Commercial_ContactUrl );
   DSIStringFree( p->Commercial_NameOfSeller );
   DSIStringFree( p->Commercial_Description );
   memset( p, 0, sizeof(*p) );
}

void DSIReadDMMAmFmTunerControlCoreTypes_engHdPsdData( DSIStream *stream, DMMAmFmTunerControlCoreTypes_engHdPsdData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIReadString( stream, &(p)->Title ) ;
   DSIReadString( stream, &(p)->Artist ) ;
   DSIReadString( stream, &(p)->Album ) ;
   DSIReadString( stream, &(p)->Genre ) ;
   DSIReadString( stream, &(p)->Comment_Language ) ;
   DSIReadString( stream, &(p)->Comment_ShortDescription ) ;
   DSIReadString( stream, &(p)->Comment_ContentField ) ;
   DSIReadString( stream, &(p)->Commercial_Price ) ;
   DSIReadString( stream, &(p)->Commercial_ValidUntil ) ;
   DSIReadString( stream, &(p)->Commercial_ContactUrl ) ;
   DSIReadString( stream, &(p)->Commercial_NameOfSeller ) ;
   DSIReadString( stream, &(p)->Commercial_Description ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_engHdPsdData( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_engHdPsdData *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWriteString( stream, &(p)->Title ) ;
   DSIWriteString( stream, &(p)->Artist ) ;
   DSIWriteString( stream, &(p)->Album ) ;
   DSIWriteString( stream, &(p)->Genre ) ;
   DSIWriteString( stream, &(p)->Comment_Language ) ;
   DSIWriteString( stream, &(p)->Comment_ShortDescription ) ;
   DSIWriteString( stream, &(p)->Comment_ContentField ) ;
   DSIWriteString( stream, &(p)->Commercial_Price ) ;
   DSIWriteString( stream, &(p)->Commercial_ValidUntil ) ;
   DSIWriteString( stream, &(p)->Commercial_ContactUrl ) ;
   DSIWriteString( stream, &(p)->Commercial_NameOfSeller ) ;
   DSIWriteString( stream, &(p)->Commercial_Description ) ;
}


void DSIReadDMMAmFmTunerControlCoreTypes_tunParamHdListThresholds( DSIStream *stream, DMMAmFmTunerControlCoreTypes_tunParamHdListThresholds *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIRead8( stream, &(p)->HD_listIn ) ;
   DSIRead8( stream, &(p)->HD_listOut ) ;
   DSIRead8( stream, &(p)->SPS_listIn ) ;
   DSIRead8( stream, &(p)->SPS_listOut ) ;
}

void DSIWriteDMMAmFmTunerControlCoreTypes_tunParamHdListThresholds( DSIStream *stream, const DMMAmFmTunerControlCoreTypes_tunParamHdListThresholds *p )
{
   if ((stream == 0) || (p == 0))
      return;
   DSIWrite8( stream, &(p)->HD_listIn ) ;
   DSIWrite8( stream, &(p)->HD_listOut ) ;
   DSIWrite8( stream, &(p)->SPS_listIn ) ;
   DSIWrite8( stream, &(p)->SPS_listOut ) ;
}




package com.harman.vehicle

import model "VehicleDataTypes.fidl"

interface PowerModeStatusProvider
{
	version { major 0 minor 1}
	
	attribute PowerModeStatus status readonly /*Description to be updated later. */
	
	enumeration PowerModeEnum
	{
		STANDARD_POWER = 0
		MODE_ON = 1
		MODE_PR = 2
		UNUSED = 3
	}
	
	struct PowerModeStatus extends com.harman.vehicle.VehicleDataTypes.VehiclePropertyBase
	{
		PowerModeEnum value
	}
}
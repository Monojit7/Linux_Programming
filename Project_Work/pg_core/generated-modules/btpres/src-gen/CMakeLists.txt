# generated by Voice CodeGen Version: R1_v1.3.0
# generated on: Fri Aug 05 17:09:10 IST 2016
#cmake ..

cmake_minimum_required(VERSION 2.6)
add_definitions($ENV{CXXFLAGS} $ENV{LDFLAGS})





include_directories(./provides)
include_directories(./consumes)

IF (WIN32)
  include_directories(../../../deliveries/commonapi/include)
ELSE()
  include_directories(${CMAKE_SYSROOT}/usr/include/CommonAPI-3.1)
ENDIF()
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -std=c++11")

OPTION(USE_INSTALLED_COMMONAPI "Set to OFF to use the local (build tree) version of CommonAPI" ON)
message(STATUS "USE_INSTALLED_COMMONAPI is set to value: ${USE_INSTALLED_COMMONAPI}")
if ("${USE_INSTALLED_COMMONAPI}" STREQUAL "ON")
endif()

message(STATUS "CommonAPI_CONSIDERED_CONFIGS: ${CommonAPI_CONSIDERED_CONFIGS}")
message(STATUS "COMMONAPI_INCLUDE_DIRS: ${COMMONAPI_INCLUDE_DIRS}")

IF (WIN32)
	include_directories(
	../../../deliveries/commonapi/include
	../../../deliveries/dbus/include
	)

	link_directories(
	../../../deploy/win/lib
)
ELSE()
	include_directories(
	${COMMONAPI_INCLUDE_DIRS}
	)
	link_directories(
	${COMMONAPI_LIBDIR}
	)
ENDIF()

#add_library(
#btpres_btpres_types_static_stub
#STATIC
#provides/v1/com/harman/btpres/PimDbHandlerStubDefault.cpp
#provides/v1/com/harman/btpres/ConnectionManagerStubDefault.cpp
#provides/v1/com/harman/btpres/CallManagerStubDefault.cpp
#)

